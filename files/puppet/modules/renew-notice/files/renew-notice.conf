server {
    listen 8117;
    root /var/www/renew-notice;

    if ($host != box) {
        rewrite .* /index.html;
    }

    location / {
        if ($host = box) {
            proxy_pass http://127.0.0.1:8000;
        }
    }
}

